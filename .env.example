# SQL Server Configuration
# Strong password required (min 8 chars, uppercase, lowercase, numbers, special chars)
MSSQL_SA_PASSWORD=YourSecurePassword123!

# JWT Configuration
# Must be at least 32 characters long for HMAC-SHA256
# Generate with: openssl rand -base64 32
Jwt__Secret=YourJwtSecretKeyAtLeast32CharactersLongForHMACSHA256

# Refresh Token Secret
# Must be at least 32 characters long
# Generate with: openssl rand -base64 32
Jwt__RefreshTokenSecret=YourRefreshTokenSecretAtLeast32CharactersLong
